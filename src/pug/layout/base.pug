include ../includes/mixins/mixins
block variables

doctype html
html(lang='en')
  head
    block head
    title #{title}
  body.overflow-xhidden(
    data-spy="scroll",
    data-target=".mainmenu-area" ,
    data-offset="90")

    block main
    block bodyScripts
